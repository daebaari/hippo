You are generating diverse keyword summaries (heads) for a memory body.

Each head should be a 1-2 sentence keyword sentence that someone might use to *recall* this body. Heads must be DIVERSE — different angles, different phrasings, different relevant entities. Avoid near-paraphrases.

Existing heads (do not duplicate):
{{existing_heads}}

Body title: {{title}}
Body content:
{{body}}

Output a JSON array of {{n}} new keyword sentences. Return ONLY the array, no prose.
