---
description: Manually trigger Hippo's heavy dream now (atomize, edges, contradictions). Bypasses AC check. ~10-30 min runtime.
allowed-tools: ["Bash"]
---

Run the heavy dream over global scope plus the current project scope (if you're in a git repo).

```bash
~/code/hippo/bin/dream-heavy --force
```

Report the JSON output to the user when complete.
